# Exclude directories that don't contain real code
--exclude=.git
--exclude=CVS

# *.inl files are cpp
--langmap=c++:+.inl

# { https://dev.mysql.com/doc/dev/mysql-server/latest/EXAMPLE_SETUP_FOR_CTAGS.html

# Index prototypes
--kinds-c++=+p

# Include language-independent fields: inherits, access and signature
--fields=+iaS

--extras=+q

--langdef=errmsg

# --regex-errmsg=/^(ER_[A-Z0-9_]+)/\1/

--langmap=errmsg:(errmsg*.txt),c:+.ic,yacc:+.yy

# }

# { SQL

--languages=+sql

# Include tags generated by guest parsers
--extras-sql=+{guest}

# }
