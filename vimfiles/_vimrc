set nocompatible              " be iMproved, required
filetype off                  " required by bundle. Just temporally

" set the runtime path to include Vundle and initialize
" Note we use backslashes so it works in mingw64, git, and windows
set rtp+=c:/Users/LDonosodeDios/.vim/bundle/Vundle.vim

" alternatively, pass a path where Vundle should install plugins
" Note we use backslashes so it works in mingw64, git, and windows
call vundle#begin()

" let Vundle manage Vundle, required
Plugin 'gmarik/Vundle.vim'
Plugin 'scrooloose/nerdtree'
Plugin 'majutsushi/tagbar'
Plugin 'tpope/vim-repeat'
Plugin 'tpope/vim-dispatch'
Plugin 'tpope/vim-surround'
Plugin 'tpope/vim-unimpaired'
Plugin 'tpope/vim-fugitive'
Plugin 'nelstrom/vim-visual-star-search'
Plugin 'nelstrom/vim-markdown-folding' 
Plugin 'christoomey/vim-tmux-navigator' 
Plugin 'flazz/vim-colorschemes'
Plugin 'mileszs/ack.vim'
Plugin 'ivanov/vim-ipython'

" All of your Plugins must be added before the following line
call vundle#end()            " required
filetype plugin indent on    " required

" Brief help
" :PluginList       - lists configured plugins
" :PluginInstall    - installs plugins; append `!` to update or just :PluginUpdate
" :PluginSearch foo - searches for foo; append `!` to refresh local cache
" :PluginClean      - confirms removal of unused plugins; append `!` to auto-approve removal
"
" see :h vundle for more details or wiki for FAQ
" Put your non-Plugin stuff after this line

" Encoding stuff
set encoding=utf-8
set fileformats=unix,dos " New files will be encoded using unix line termination.


set tabstop=4 shiftwidth=4 softtabstop=4 expandtab   
syntax on
set number
set wildmode=longest,list
set mouse=a
set cursorline

" Search stuff
set incsearch hlsearch
set ignorecase smartcase
" conflicts with tmux navigator
" nnoremap <silent> <C-l> :nohlsearch<CR><C-l>

if has("gui_running")
    set guioptions -=T
endif

set colorcolumn=100
" enable degraded color mode. This is needed only if you do not apply solarized colors to your terminal emulator.
let g:solarized_termcolors=256
set background=dark
colorscheme solarized

set hidden

" tags. One tag file per directory and a general one
" in the working directory with the non-static definitions
set tags=./.tags,.tags 
map ,t :!(cd %:p:h;ctags -f .tags *)& 

" Windows
set splitright
set splitbelow

" Center the display line after searches. (This makes it *much* easier to see the matched line.)
" More info: http://www.vim.org/tips/tip.php?tip_id=528
nnoremap n nzz
nnoremap N Nzz
nnoremap * *zz
nnoremap # #zz
nnoremap g* g*zz
nnoremap g# g#zz

" Enter command mode with one keystroke
nnoremap ; :
nnoremap : ;

" vimdiff colorscheme
if &diff
    colorscheme github
endif

" tagbar slowness
autocmd FileType tagbar setlocal nocursorline nocursorcolumn

" Use silver searcher when available
if executable('ag')
    let g:ackprg = 'ag --vimgrep'
endif

" Marex
set makeprg=scons\ opt=debug,test

" Map leader remap
let mapleader = "\<Space>"

" Automatically jump to end of text you pasted
vnoremap <silent> y y`]
vnoremap <silent> p p`]
nnoremap <silent> p p`]
