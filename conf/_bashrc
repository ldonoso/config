set -o vi

alias g='gvim --remote-silent'

if [ -n "$DISPLAY" -a "$TERM" == "xterm" ]; then
    export TERM=xterm-256color
fi

export EDITOR=vim

alias to_md='pandoc -t markdown_github --wrap=none --atx-headers'
